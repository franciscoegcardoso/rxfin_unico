/**
 * React Router DOM v6 re-exports for consistent usage across the app.
 * (Previously a Next.js compatibility shim — removed; app uses Vite + React Router only.)
 */
export {
  Link,
  NavLink,
  useNavigate,
  useLocation,
  useParams,
  useSearchParams,
  Navigate,
  Outlet,
} from 'react-router-dom';
