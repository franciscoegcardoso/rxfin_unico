/**
 * Re-export do layout oficial. O shell do app (TopNavbar + main) deve ser
 * renderizado em um único lugar — use @/components/layout/AppLayout.
 */
export { AppLayout } from '@/components/layout/AppLayout';
