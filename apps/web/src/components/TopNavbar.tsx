/**
 * Re-export da navbar oficial (com menu mobile/hamburger). O menu deve ser
 * renderizado em um único lugar — use @/components/layout/TopNavbar.
 */
export { TopNavbar } from '@/components/layout/TopNavbar';
