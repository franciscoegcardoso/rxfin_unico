-- Upload logo placeholder: we need to reference the public URL
-- The actual upload will be done via the storage-upload tool
-- This is just to ensure the bucket exists (already created above)
SELECT 1;