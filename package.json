{
  "name": "rxfin",
  "version": "1.0.0",
  "private": true,
  "description": "RXFin - Plataforma de finanças pessoais",
  "workspaces": [
    "apps/web-next",
    "apps/web",
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=web-next",
    "dev:web": "turbo run dev --filter=web",
    "dev:mobile": "cd apps/mobile && npx expo start",
    "build": "turbo run build --filter=web-next",
    "build:web": "turbo run build --filter=web",
    "build:all": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "supabase:functions:deploy": "supabase functions deploy --project-ref $SUPABASE_PROJECT_ID"
  },
  "devDependencies": {
    "turbo": "^2",
    "supabase": "^2.76.15"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.0.0"
}
